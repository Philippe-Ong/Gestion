# AGENTS.md — ThéCol Gestion

Guidance for agentic coding agents working in this repository.

## Project Overview

ThéCol Gestion is a single-page business management app for a cold tea company. The entire UI is in French. It is hosted on GitHub Pages with no build step, no bundler, and no framework.

## Commands

There is **no build, lint, typecheck, or test pipeline**. The project has zero dependencies to install.

To serve locally:

```bash
npx http-server
# or
python -m http.server 8000
```

Open the printed URL in a browser. There is nothing to compile or bundle.

## File Structure

| File | Role |
|------|------|
| `index.html` | Page shell, modal/toast containers, Firebase SDK init (inline module script) |
| `app.js` (~3700 lines) | All application logic: routing, views, data layer, modals, CRUD operations |
| `styles.css` (~1750 lines) | All styling, CSS custom properties, responsive breakpoints |
| `SPEC.md` | Data schemas and feature specifications |
| `templates/` | Excel templates (e.g. `bl_template.xlsx`) |

No other files contain application logic. Do not create new JS or CSS files unless explicitly asked.

## Architecture

### Routing

Hash-based SPA routing. Pages: `#dashboard`, `#stock`, `#pointage`, `#commandes`, `#livraisons`, `#production`, `#inventaire`, `#parametres`, `#archives`.

The `navigateTo(page)` function looks up the page name in a views map and calls the matching `render<Page>()` function, which generates HTML and writes it to `#content` via `innerHTML`.

### View Pattern

Every page has a top-level `const render<Page> = () => { ... }` arrow function that:
1. Reads data from `DB.get(key)`
2. Builds an HTML string with template literals
3. Writes it to `document.getElementById('content').innerHTML`
4. Attaches event listeners after rendering

### UI Components

- **Modals:** `modal.show(title, bodyHtml, footerHtml)` / `modal.hide()`
- **Toasts:** `showToast(message, type)` where type is `'success'` | `'error'` | `'warning'`
- **All SVG icons** are inline `<svg>` elements (no icon library)

## Code Style

### JavaScript

- **No ES modules in `app.js`** — it is loaded as a plain `<script>`. Firebase imports use dynamic `import()` or the inline `<script type="module">` in `index.html`.
- **Arrow functions** for all declarations: `const fn = () => { }`
- **camelCase** for all JS identifiers: variables, functions, object properties
- **Template literals** (backticks) for all multi-line HTML strings
- **IDs** generated by `generateId()` — returns `'_' + Math.random().toString(36).substr(2, 9)`
- **Dates** formatted with Swiss French locale: `fr-CH` via `toLocaleDateString('fr-CH')`
- **No comments** in code — keep it clean unless the user asks for them

### Error Handling

- Wrap JSON parsing and async operations in `try/catch`
- Log errors with `console.error('Descriptive prefix:', e)`
- Return safe defaults on failure (e.g., `return []` for data reads)
- Show user-facing errors via `showToast('message', 'error')`

### CSS

#### Design Tokens (CSS Custom Properties)

All theme values are defined in `:root` in `styles.css:1-26`. Always use these variables — never hard-code colors or radii.

| Token | Value | Usage |
|-------|-------|-------|
| `--primary` | `#5D7B3E` | Main green (olive) — buttons, active states |
| `--primary-dark` | `#4A6530` | Hover state for primary |
| `--primary-light` | `#8BA66B` | Secondary green |
| `--accent` | `#2C4A2E` | Dark green accent |
| `--bg` | `#FAFBF7` | Page background (cream white) |
| `--bg-secondary` | `#F5F7F2` | Card backgrounds, secondary areas |
| `--danger` | `#C0392B` | Destructive actions |
| `--warning` | `#F39C12` | Warning states |
| `--success` | `#27AE60` | Success states |
| `--radius` | `8px` | Standard border-radius |
| `--radius-lg` | `12px` | Cards, large containers |
| `--shadow` | `0 2px 8px rgba(0,0,0,0.08)` | Card elevation |
| `--transition` | `all 0.2s ease` | Standard transition |

#### Component Class Naming

Use BEM-like flat naming (no nested BEM). Examples from the codebase:

- **Buttons:** `.btn` (base), `.btn-primary`, `.btn-secondary`, `.btn-danger`, `.btn-success`, `.btn-sm`, `.btn-icon`
- **Cards:** `.card`, `.card-header`, `.card-title`, `.card-actions`
- **Stats:** `.stats-grid`, `.stat-card`, `.stat-icon`, `.stat-content`
- **Tables:** `.table-container`, `.table-actions`, `.actions-cell`
- **Forms:** `.form-group`, `.form-row`, `.form-group-btn-end`
- **Modals:** `.modal-overlay`, `.modal`, `.modal-header`, `.modal-body`, `.modal-footer`, `.modal-close`
- **Toasts:** `.toast-container`, `.toast`, `.toast.success`, `.toast.error`, `.toast.warning`
- **Status:** `.status-badge`, `.status-dropdown`, `.status-option`

#### Utility Classes

Defined at `styles.css:1132-1140`:

- `.flex` — `display: flex`
- `.flex-between` — flex with space-between + center alignment
- `.gap-2` / `.gap-4` — 8px / 16px gap
- `.text-center` / `.text-right` / `.text-muted`
- `.mt-4` / `.mb-4` — 16px margin top/bottom

#### Responsive Breakpoints

- `@media (max-width: 1024px)` — tablet adjustments (sidebar collapse, form rows stack)
- `@media (max-width: 768px)` — mobile: sidebar becomes drawer, cards go full-width, buttons stack vertically

When adding new styles, place responsive overrides inside the existing `@media` blocks at the bottom of `styles.css` rather than creating new ones.

#### Adding New Styles

- Use existing CSS variables, not magic numbers
- Follow the established component pattern: base class + modifier classes
- Use `var(--transition)` for all hover/focus transitions
- Font: `'Outfit', sans-serif` (inherited from `body`)
- Font sizes: 13px (small), 14px (standard), 15px (body), 18px (card titles), 22px (page headers), 28px (stats)
- Padding rhythm: 4px, 8px, 12px, 16px, 20px, 24px, 32px

## Data Layer

### localStorage

The `DB` object wraps localStorage. All keys are prefixed `thecol_`:

```
thecol_employees, thecol_aromes, thecol_formats, thecol_recettes,
thecol_clients, thecol_lots, thecol_commandes, thecol_pointages,
thecol_inventaire
```

- **Read:** `DB.get('lots')` — returns parsed array, defaults to `[]` on error
- **Write:** `DB.set('lots', data)` — serializes to JSON, auto-syncs to Firebase if connected

### Firebase

Optional cloud sync via Firestore. Config is in `index.html` inline script. Sync is manual (user clicks the Sync button). Firebase modules use dynamic `import()` from CDN.

## Naming Conventions

| Context | Convention | Example |
|---------|-----------|---------|
| JS functions | camelCase, arrow const | `const renderStock = () => {}` |
| JS variables | camelCase | `const totalBouteilles = ...` |
| CSS classes | lowercase, hyphen-separated | `.btn-primary`, `.stat-card` |
| CSS variables | `--kebab-case` | `--primary-dark`, `--bg-secondary` |
| Data keys | lowercase English | `'employees'`, `'commandes'`, `'lots'` |
| Status values | French with underscores | `'en_attente'`, `'livree'`, `'annulee'` |
| UI text | French | All labels, buttons, toasts, headings |
| Page routes | Lowercase French | `#commandes`, `#pointage`, `#parametres` |

## Important Rules

- **All UI text must be in French** — labels, buttons, toasts, confirmations, headings, error messages
- **Do not add comments** to code unless the user explicitly requests them
- **Never commit secrets** — Firebase config in index.html is public (client-side SDK); do not add server keys
- **Do not introduce frameworks, build tools, or npm packages** — this project intentionally uses vanilla HTML/CSS/JS
- **Do not create new JS or CSS files** unless asked — all logic goes in `app.js`, all styles in `styles.css`
- **Preserve the data schema** defined in `SPEC.md` — any new fields should be added as optional properties
- **Stock status** is auto-computed from DLC dates: `ok`, `warning` (< 1 month), `expired` (past)
- **Production planner** aggregates orders by date range, calculates litres per arôme, applies recipes to compute ingredients
- When editing inline HTML in `app.js`, match the existing indentation style within template literals
